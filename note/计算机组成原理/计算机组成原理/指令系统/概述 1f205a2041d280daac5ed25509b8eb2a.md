# 概述

所有者: H34V3N

一些基本概念

机器指令：机器能直接识别，执行的某种指令

指令系统（指令集）：一台机器所有机器指令的集合

*一般 intel公司指令集有300多条指令*

系列机：基本指令系统和基本结构相同的计算机，能解决软件兼容的问题

在之前我们学过计算机指令系统的层次，里面就有一个指令集架构层，这一层信息对应的是二进制，每一条二进制都对应着汇编语言的信息

指令系统是cpu设计的依据，软件设计的基础，硬件和软件的界面，其直接影响计算机系统的性能，指令集在cpu中，将转换成二进制的指令读取到指令集寄存器，cpu进行译码，再执行指令