# 写回法

所有者: H34V3N

使用写回法时，cpu先把数据写进cache中，修改cache的内容，而不立即写到主存里，当cache这一行的数据被替换时，才会把这段数据写进主存

实现这种方式，需要在每个cache行专门上一个修改位，我们称其为**脏位**。来标识此行有没有被改写

当这一行被换出时，如果脏位为1，那么这行数据就不会被简单的丢掉，而是根据tag等信息被写入主存对应的位置

这种方式保证读写都高速运行，但是会导致cache和主存信息的不一致性，导致dma得到的不是最新的数据