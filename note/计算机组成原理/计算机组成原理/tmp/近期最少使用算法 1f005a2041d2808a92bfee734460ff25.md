# 近期最少使用算法

所有者: H34V3N

和最不经常使用相似，每一行同样会加一个计数器，记录没被访问的次数

如果a行被访问了，那么a行不动，除a行外的所有计数器加1

当需要替换数据时，选取计数最大的行被替换

这样可以保护cache刚载入的数据不会替换，不会出现刚进来就因为使用次数太低而被替换的情况

![image.png](近期最少使用算法/image.png)

与别的相比，这个查找的算法相对复杂