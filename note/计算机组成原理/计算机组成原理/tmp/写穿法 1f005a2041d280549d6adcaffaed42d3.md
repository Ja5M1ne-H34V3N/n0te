# 写穿法

所有者: H34V3N

写穿法也成为直写法，当cache命中时，将会同时修改cache和主存的信息，这样的话，就不用设置类似脏位的修改位，被换出的数据直接丢了就行

只是，写穿法写了就会改主存的数据，这样导致写没有缓冲的功能，而且，多核cpu每个cpu都有一个cache，这样就导致如果每个cpu都同时对一个位置写数据，虽然主存中对应信息是按照所有cpu改变的，但是每个cpu在cache里看到的只有自己修改过的信息的样子，会导致各自的cache和主存中的信息不同的情况